import { ToTypeIconPipe } from './to-type-icon.pipe';

describe('ToTypeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ToTypeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
